apiVersion: v1
kind: Service
metadata:
  name: {{ include "agentsync.fullname" . }}-web
  labels:
    {{- include "agentsync.web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.web.service.type }}
  ports:
    - port: {{ .Values.web.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agentsync.web.selectorLabels" . | nindent 4 }}
