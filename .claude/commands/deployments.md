---
description: Show current deployment status and identify issues
---

Use the agentsync skill to check all deployments and provide a summary.

Show me:
1. Total number of deployments by status (running, completed, failed, pending)
2. Details of any failed deployments including error messages
3. Status of currently running deployments with progress
4. Any deployments that need attention (stuck, stale, requiring approval)

Present the information in a clear, actionable format. If there are failures, suggest next steps (retry, check logs, verify tenant health).
