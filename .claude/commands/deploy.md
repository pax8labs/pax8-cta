---
description: Deploy an agent to specified tenants
---

Help me deploy a solution using the agentsync CLI.

Ask which solution and which tenants if not specified, then run the appropriate `agentsync deploy` command. Use `--direct` for sequential deployment. Monitor with `agentsync deployments watch <id>` after.
