---
description: Deploy an agent to specified tenants
---

Help me deploy a solution using the pax8-cta CLI.

Ask which solution and which tenants if not specified, then run the appropriate `pax8-cta deploy` command. Use `--direct` for sequential deployment. Monitor with `pax8-cta deployments watch <id>` after.
